#include <stdio.h>

int main() {
	int n1, n2, sum;
	printf("Please enter numbers: ");
	scanf("%d %d", &n1, &n2);
	sum = n1 + n2,
	printf("The sum of the numbers %d, and %d is: %d\n", n1, n2, sum); 
	return 0;
}
